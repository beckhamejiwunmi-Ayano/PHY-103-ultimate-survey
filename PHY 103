let history = JSON.parse(localStorage.getItem(user) || "[]"); let last = history.length ? history[history.length-1] : null;  history.push(percent); localStorage.setItem(user, JSON.stringify(history));  let attempt = history.length; let best = Math.max(...history);  // ================= STREAK ================= let streak = Number(localStorage.getItem(user+"_streak")||0); if(percent >= PASS_MARK){   streak++; }else{   streak = 0; } localStorage.setItem(user+"_streak", streak);  // ================= MEDAL ================= let medal = ""; if(percent >= 90) medal = "🥇 Gold"; else if(percent >= 75) medal = "🥈 Silver"; else if(percent >= PASS_MARK) medal = "🥉 Bronze";  // ================= MESSAGE ================= let message=""; if(percent >= 90) message="Unstoppable genius!"; else if(percent >= 75) message="Monster growth!"; else if(percent >= PASS_MARK) message="You passed. Push higher!"; else message="Again. Break your limit!";  review = ` <h2>Result</h2> <p>${user}</p> <p>Score: ${score}/${questions.length}</p> <p>Percentage: ${percent}%</p> <p><b>${percent>=PASS_MARK?"PASS":"FAIL"}</b></p>
